# Prompt 筛选清单 — 用于报告「关注 AI 引导 + 体验反馈修改」主题

> 以下 12 条候选 Prompt 按时间顺序排列，覆盖了「AI 引导设计逐步成型 → v1.0 实现后基于实际体验反馈修改 → 持续迭代打磨」的完整过程。
> 每条标注了：**你的原话 / AI 的引导方式 / 后续迭代 / 为什么重要**。
> 报告建议精选 6-8 条。

---

## 一、AI 引导设计阶段（逐步成型）

### 候选 1：从「温润」到「温润 × 锐气」—— AI 帮你用矛盾框架做设计

**你的原话**：
> 温润的同时，我希望能让网页设计带有一些锐气，即是网页的设计应当有一些高端的效果（各种动态效果等），让这个网页显得更加具有设计感和交互感。

**AI 的引导**：AI 没有直接写代码，而是提炼了一个设计框架——「温润是材质，锐气是工艺」。温润落在色彩/字体/留白，锐气落在视差滚动、光标交互、动效精度。并给出了动画原则：快入慢出（0.3~0.8s）、克制用量、每个动效有意义。

**为什么重要**：这是**整个设计的原点**。AI 没有直接写代码，而是先帮你建立设计语言。这是「设计优先」策略的起点。

---

### 候选 2：AI 主动引导结构设计——六个区域的确定

**AI 的引导方式**：AI 主动提出「单页叙事 + 六大区域」的页面骨架方案，并用情绪曲线图展示了浏览节奏（首屏好奇心 → 关于放松 → 思考专注 → 手记浏览 → 联系收束 → 页脚彩蛋）。然后逐区域让用户确认。

**你的回应**：逐区域确认，提出了关键需求——「思考区做横向翻阅」。

**为什么重要**：体现了 **AI 主导结构设计、你做决策**的协作模式。AI 给出的不是代码而是方案，你不需要自己设计骨架。

---

### 候选 3：AI 给出「横向翻阅」选项，你选择思考区

**你的原话**：
> 能不能在某一个板块，将网页浏览的垂直滑动视觉效果替换为横向滑动，即是在网页继续向下滑的过程中，某一个板块视觉上是横向的滑动，像书页条目一样一页一页略过。

**AI 的引导**：AI 分析了六个区域各自适合横向滑动的程度（首屏❌ / 关于❌ / 思考⭐⭐⭐ / 手记⭐⭐ / 联系❌ / 页脚❌），推荐在思考区做横向翻阅——「文章=书页」隐喻最贴切。

**为什么重要**：你的需求是一个**比喻**（「像书页翻过」），AI 将这个比喻**翻译成了具体的技术方案**（ScrollTrigger pin+scrub 横向翻阅），并给出了选项让你选择。这是「AI 翻译设计意图」的典型场景。

---

### 候选 4：兴趣标签 —— AI 先给方案，你再确定风格

**AI 的引导**：AI 先提出了「兴趣标签点击展开数据面板」的交互方案。你确认后，AI 追问「游戏/音乐/影视/动漫分别想用什么风格？」

**你的原话**：
> 游戏标签：像素风；音乐标签：波形动画加环形唱片；影视：电影票；动漫：小电视机。

**AI 的回应**：为每个标签设计了独立的主题面板（像素游戏机 + 精灵走动 / 黑胶唱片 + 声波 / 电影票根 + 撕边 / CRT 小电视 + 弹幕）。

**为什么重要**：体现「AI 先搭框架 → 你再填细节」的分工。AI 提出交互方案，你用简单的关键词确定风格方向，AI 完成具体实现。

---

### 候选 5：页脚彩蛋 —— AI 提选项，你组合选择

**AI 的引导**：AI 列出了 5 个彩蛋方向（隐藏一句话 / 连续点击触发 / 时间感知 / 鼠标轨迹 / 隐藏歌单），并给出了推荐组合。

**你的原话**：
> 认可你说的一和三组合，可以的话也可以加入隐藏歌单。

**AI 的回应**：将三个彩蛋整合进页脚设计，按发现难度分层（★☆☆ 时间问候 / ★★☆ 隐藏歌单 / ★★★ 隐藏一句话）。

**为什么重要**：AI 给了**带推荐的选择题**而非开放式问题，降低了你的决策难度。你只需要在 AI 的框架上做加减法。


## 二、v1.0 实现后的体验反馈修改（核心章节）

### 候选 6：v1.0 出炉后的第一次反馈 —— 回到顶部按钮看不到 + 导航平滑过渡

**你的原话**：
> 我没有看到回到顶部的交互按钮，同时希望能够添加到各种区域的切换动画，而不是直接传送到指定区域。

**AI 的回应**：将回到顶部按钮从隐形小字改为 40px 圆形按钮（带边框和悬停效果），导航链接从 `href="#about"` 直接跳转改为 JavaScript `scrollTo({ behavior: 'smooth' })`，侧边圆点也加上点击平滑滚动。

**为什么重要**：这是 **v1.0 实现后第一次实际体验反馈**。AI 按设计文档写完代码，但实际效果和预期有差距——按钮不显眼、跳转太生硬。这两点都是 AI 在设计阶段无法预判的，只有在浏览器里实际使用才能发现。

---

### 候选 7：关于区标签提示不足 —— 从无提示到永久标签

**你的原话**：
> 标签提示不要点击后消失，应当一直存在。同时为关于区我的兴趣的标签交互增加提示（点开可以看到个人数据）。

**AI 的回应**：第一版加了「戳我看看」浮动提示（3s 消失）+ ▼ 小箭头。你说要永久存在，AI 改为左侧永久标签「点开查看 →」，一行文字始终指向四个兴趣标签。

**为什么重要**：体现了 **v1.0 的可用性问题**——AI 在设计时知道标签可以点击，所以没有加提示；但实际访客（没有看过设计文档的人）不知道这是可交互的。这个反馈来自「换位思考」——站在访客视角发现缺失。

---

### 候选 8：联系区图标行为修正

**你的原话**：
> 联系区的时钟图标会直接跳到主页，这样不好。社交区的邮件图标我希望能够弹出我的邮箱即可，不要跳转到其它网页。

你现在还补充了：**音乐图标看着更像时钟图标**。

**AI 的回应**：邮箱从 `mailto:` 改为点击复制地址+Toast「已复制」；QQ 音乐从 `#` 空链接改为滚动到页脚歌单+自动展开；时钟图标改为双八分音符 SVG。

**为什么重要**：这是**三重反馈叠加**——①邮箱打开邮件客户端的体验不好（很多人电脑没配邮箱）②QQ 音乐图标没有实际功能（空链接跳到顶部）③图标本身是时钟而不是音符（视觉误导）。三条反馈证明了「设计师在代码里看不出体验问题，只有实际点击才能发现」。

---

### 候选 9：卡片光泽太刺眼 —— 从「默认显示」到「仅 hover」

**你的原话**：
> 卡片光泽扫过效果实际体验比较刺眼，hover 时带上光泽即可。

**AI 的回应**：光照从默认显示改为仅 hover 触发，最高亮度从 `rgba(255,255,255,0.25)` 降至 `0.14`，位移范围缩小，过渡延长到 1.2s ease-out。同时修复了光泽与折角、渐变遮罩共用伪元素的冲突。

**为什么重要**：这是一个**参数级反馈**——功能是对的（有光泽扫过效果），但参数不对（太亮、一直在闪）。AI 在写代码时默认了一个参数值，但那个值在屏幕上实际看起来是刺眼的。这种级别的反馈只能通过「实际看了效果」才能给出。

---

### 候选 10：「不是标题居中，是卡片居中」——纠正 AI 的理解偏差

**你的原话**：
> 不是要随笔两个字放在中间，而是要随笔的卡片放在中间，随笔仍然在顶部。

**AI 的回应**：AI 之前把「卡片放在中间」理解成了将「随笔」标题 `position: fixed; top: 50%` 居中固定在屏幕上。修正后标题恢复顶部自然位置，卡片 track 容器用 `flexbox align-items: center` 实现垂直居中。

**为什么重要**：这是 **Vibe Coding 中「纠错」的经典案例**。AI 理解错了对象（标题 vs 卡片），因为你说的「放在中间」有歧义。你能立即发现不对、用精确的否定+重述纠正——这种纠错能力是驾驭 AI 的核心技能。


## 三、v1.0 → v2.0 升级决策

### 候选 11：从「网页太素」到视觉填充 —— 植物放大+几何线条

**你的原话**：
> 我认为现在网页整体视觉太空了，植物素材在其中站位和存在感太小，不仔细观看都难以注意。需要更加大胆一些，可以大胆使用线条和几何图形。

**AI 的回应**：AI 提出了全面的「视觉填充方案」——植物放大 2.5 倍（60-80px → 120-160px），新增边缘竖线、首屏菱形线、四角 L 形边框、标题圆点、章节菱形分隔、卡片折角、联系区双层圆环等十几项几何装饰。

**为什么重要**：这是 v1.0 实现后**最大的一次视觉改版**。之前的两个版本功能到位但视觉空洞——这个问题 AI 在设计阶段没有预判到，你也是在看了成品之后才清晰感受到「空间」。

---

### 候选 12：手帐背景增强 —— 六套方案全部用 CSS 实现

**你的原话**：
> 背景能不能在保留手帐感的同时添加更多元素。

**AI 的回应**：AI 提出六套手帐元素方案（和纸胶带 / 印章邮戳 / 手绘线条 / 碎纸片 / 日付页码 / 翘角贴纸），全部用 CSS 实现，不需要新增图片。

**为什么重要**：这是「设计优先」策略的**延伸优势**——当基础骨架正确后，加装饰层可以非常高效。手帐六套方案的 Prompt 只有一句话，但 AI 产出了 300+ 行 CSS——因为设计基调已经锁定，AI 知道「往哪个方向加」。


## 筛选建议

如果你要聚焦 **「AI 如何引导我一步步确定设计」**，保留候选 1、2、3、4、5。

如果你要聚焦 **「v1.0 实现后我如何根据体验反馈修改」**，保留候选 6、7、8、9、10。

两者结合——建议最终精选 **6-8 条**：1、3、5、6、9、10（覆盖定调→交互→彩蛋→反馈→纠错）。如果篇幅够再加 4 和 11。

---

## 你当前报告中已有的 Prompt（需要对比替换）

| 报告现有 | 对应的候选 | 改动 |
|:-----|:--:|:-----|
| Prompt ① 确立调性 | 候选 1 | 保留，微调强调 AI 引导 |
| Prompt ② 招牌交互 | 候选 3 | 保留 |
| Prompt ③ 数据可视化 | 候选 4 | 保留 |
| Prompt ④ 彩蛋系统 | 候选 5 | 保留 |
| Prompt ⑤ 纠正偏差 | 候选 10 | **保留，这是最精彩的** |
| Prompt ⑥ 视觉升级 | 候选 11 | 保留 |
| Prompt ⑦ 手帐背景 | 候选 12 | 保留 |
| Prompt ⑧ 细节打磨 | 候选 9 | 保留 |
| （缺） | 候选 6（回顶+导航） | **建议新增** |
| （缺） | 候选 7（标签提示） | 可选新增 |
| （缺） | 候选 8（图标修正） | 可选新增 |
