# 个人主页 v2.0 设计计划

> 版本：v2.0
> 日期：2026年6月
> 状态：设计讨论中，待实现

---

## 设计理念

> v1.1：温润材质 × 锐利工艺
> v2.0：**温润材质 × 锐利工艺 × 生命律动**

v2.0 的核心目标：在保持温润克制的基础上，为页面注入**生机活力**——让页面像午后阳光下的书房，安静但不死寂，有光、有风、有生命的脉搏。

### 设计原则

| 不是 | 而是 |
|:-----|:-----|
| 加很多颜色（花） | 让现有颜色之间有**过渡和流动** |
| 加很多动效（闹） | 让动效**像呼吸一样自然**，永远在动但从不抢戏 |
| 加很多装饰素材（乱） | 引入**有意义的视觉元素**——植物、光影、手绘线条 |
| 静态的卡片 | **活着的内容**——数据有脉搏、标签有心跳、卡片像被风吹过的纸 |

---

## 五大升级模块

### 模块 A：数据可视化（优先实现）⭐⭐⭐

将关于区的四个兴趣标签从纯文字数据升级为**微型互动场景**。

#### A1. 🎮 游戏 — 像素风

**视觉风格**：复古像素游戏机/卡带风格。

点击标签后，数据面板不再是纯文字，而是一个**像素风卡片面板**：

- 背景模拟 GameBoy 绿色屏幕的极淡质感，或像素风格的柔和配色
- "洛克王国世界"以像素字体呈现
- 三行数据排列成卡带插槽上的标签样式
- 面板角落有一个 8-bit 风格的小角色在走动（极简像素动画，4帧循环）

**数据呈现**：

```
┌──────────────────────────────┐
│  ┌──────────────────────┐   │
│  │  最近在玩：洛克王国世界 │   │  ← 像素边框
│  │  最爱类型：模拟经营·RPG │   │
│  │  游戏年龄：物理年龄-7年 │   │
│  └──────────────────────┘   │
│                    🦊 👾     │  ← 8-bit 像素小角色走动
└──────────────────────────────┘
```

---

#### A2. 🎵 音乐 — 波形动画 + 环形唱片

**视觉风格**：极简唱片机/黑胶风格。

点击标签后，数据面板呈现一个**微型唱片播放器**：

- 中央一个环形唱片在极慢速旋转（CSS animation，约 8 秒一圈）
- 唱片中心写着"241首"（最突出的数字）
- 唱片外围有微弱的声波线（SVG 或 CSS）随着滚动微微起伏
- "Blue Sky, Blue Star"以手写体印在唱片标签区

**数据呈现**：

```
┌──────────────────────────────────┐
│                                  │
│        ╭───────────╮             │
│       ╱ ╲  ┌──┐   ╱ ╲           │  ← 声波纹路（微弱跳动）
│      │ ○  │241│  ○  │           │
│      │    │ 首 │     │           │  ← 环形唱片（慢速旋转）
│       ╲  └──┘    ╱             │
│        ╰─────────╯              │
│                                  │
│  最近单曲：Blue Sky, Blue Star   │  ← 手写体
│  五月听歌：861 首 · QQ 音乐      │
└──────────────────────────────────┘
```

---

#### A3. 🎬 影视 — 电影票

**视觉风格**：老式电影票根/放映机票。

点击标签后，数据面板呈现一张**复古电影票**：

- 票根纵向排列（像从电影院撕下来的票根）
- 每张票上以手写体或打字机体印着电影名：《给阿嬷的情书》《挽救计划》
- 票根边缘有锯齿状裁剪痕迹（CSS clip-path 或 border 实现）
- 票根背景是微黄的纸色，比页面底色更"旧"
- "杂食类，除了喜剧"作为票根底部的印章文字

**数据呈现**：

```
┌────────────────────┐
│  🎬  ADMIT ONE     │
│                    │
│  ✂ - - - - - - -  │  ← 锯齿撕边
│                    │
│  《给阿嬷的情书》   │  ← 手写/打字机体
│  《挽救计划》       │
│                    │
│  杂食类，除了喜剧   │  ← 印章风格
│                    │
│  ✂ - - - - - - -  │
└────────────────────┘
```

---

#### A4. 📺 动漫 — 小电视机

**视觉风格**：复古 CRT 小电视/显示器。

点击标签后，数据面板呈现一台**极简线条小电视机**：

- 电视机外壳是圆润的线条框（CSS 绘制）
- 屏幕区域显示推荐的动漫名，以像素或手写字体呈现
- "数不清咯"在屏幕下方像字幕一样浮现
- 《末日后酒店》在屏幕中央以标题卡的形式呈现，附带一个微小的星星闪烁动画
- "都给我去看！"以弹幕风格从右到左飘过屏幕

**数据呈现**：

```
┌────────────────────────────┐
│  ┌──────────────────────┐  │
│  │                      │  │  ← 电视机外壳
│  │  《末日后酒店》       │  │
│  │  ★ 都给我去看！★     │  │  ← 屏幕区（微闪星星 + 弹幕飘过）
│  │  ── 字幕：数不清咯 ── │  │
│  │                      │  │
│  └──────────────────────┘  │
│     📺  最近在追：          │
│     《正相反的你和我》       │
└────────────────────────────┘
```

---

### 模块 B：生命律动系统（优先实现）⭐⭐⭐

#### B1. 粒子生态系统

**描述**：首屏背景加入极简漂浮粒子（10~15个），像阳光中的微尘或夏夜萤火虫。

**行为**：
- 极其缓慢地漂移（随机方向 + 随机速度，周期 8~15 秒）
- 鼠标划过时会轻微避让（排斥力），然后慢慢聚回原位（吸引力）
- 粒子颜色随时间渐变（清晨冷白 → 午后暖金 → 夜晚微蓝）
- 每个粒子有微弱的透明度脉动（60% → 90% → 60%，周期 3~5 秒）
- 数量克制，不密集——能被感受到但不被注意到

**技术**：Canvas 或纯 CSS + JS。推荐 Canvas，性能更好。

---

#### B2. 植物/自然元素

**描述**：在页面角落和过渡处加入极简线条植物 SVG 插画。

**布局**：
| 位置 | 元素 | 效果 |
|:-----|:-----|:-----|
| 关于区旁 | 一株小盆栽 | 随滚动有极微弱的摇摆（像被风吹过） |
| 手记区旁 | 一支藤蔓 | 同上 |
| 页脚旁 | 一棵小树苗 | 同上 |
| 思考区标题旁 | 一片小叶子 | 同上 |

**视觉风格**：极简线条画（outline only），颜色使用 `var(--accent-alt)`（苔藓绿）或 `var(--text-muted)`（浅灰棕），透明度和当前的装饰元素一致。

**素材来源**：可以收集 SVG 素材，也可以由设计师用 CSS/SVG 手绘。

**动画**：使用 GSAP 的 `gsap.to()` 配合 `repeat: -1, yoyo: true`，做极缓慢的左右摇摆（±3°，周期 5~6 秒）。

---

#### B3. 呼吸感动画系统

| 元素 | 动画 | 周期 |
|:-----|:-----|:-----|
| 首屏名字 | 极慢 scale 波动 `102% → 100% → 102%` | 4 秒 |
| 标签（未选中时） | 微弱的 opacity 波动 `0.72 → 0.85 → 0.72` | 3 秒 |
| 滚动引导箭头 | 不是单调弹跳，而是像水母一样的起伏 `translateY + scale` | 3 秒 |
| 区域标题装饰线 | 极其缓慢的左右摆动 | 6 秒 |
| 粒子 | 透明度脉动 | 3~5 秒 |

---

### 模块 C：游戏与彩蛋系统 ⭐⭐

#### C1. 隐藏贪吃蛇小游戏

**触发**：在页脚的 `~` 装饰符上连续点击 5 次（1 秒内）。

**触发后**：
- 整个页面叠加一层半透明暗色遮罩（`rgba(61, 50, 43, 0.7)`）
- 中央浮现一个"桌面纸"区域
- 贪吃蛇使用页面色板：蛇身=陶土色圆点（`var(--accent)`），食物=苔藓绿（`var(--accent-alt)`）
- 边框使用 `var(--card-shadow-hover)` 风格阴影
- 得分用页脚手写体显示
- 方向键或 WASD 控制
- 死亡后显示最终得分 + 历史最高分（localStorage）+ "再来一次？"按钮
- 点击背景或 ESC 退出

**移动端适配**：滑动手势控制方向（swipe detection）。

---

#### C2. 彩蛋成就系统

**入口**：页脚加入一个极小的、几乎不可见的图标（闭着的眼睛 `👁️‍🗨️` 或一个极淡的 `?`）。

**点击后展开彩蛋面板**：

| # | 成就名称 | 描述 | 解锁条件 |
|:--|:-----|:-----|:-----|
| 🥚 | 时间旅人 | 见过清晨、白天、傍晚、深夜的主页 | 在四个时间段各访问一次 |
| 🥚 | 音乐鉴赏家 | 完整的听过一首歌 | 听完一首隐藏歌 |
| 🥚 | 秘密读者 | 发现页脚的隐藏信息 | 发现页脚隐藏一句话 |
| 🥚 | 留言达人 | 留下自己的话语 | 写一条便签留言 |
| 🥚 | 游戏高手 | 挑战贪吃蛇的小成就 | 贪吃蛇分数超过20分 |
| 🥚 | 深夜访客 | 在深夜浏览主页 | 凌晨 0:00-4:00 访问 |

**视觉**：每个未解锁的彩蛋显示为灰色轮廓 + `???`；解锁后亮起对应颜色，显示成就名称。解锁时弹出一个小小的庆祝动画（彩色纸屑从成就图标中散出）。

**存储**：localStorage。亲友之间可以比较解锁数量。

---

### 模块 D：深度视觉升级 ⭐⭐

#### D1. 排版艺术

| 元素 | 当前 | v2.0 |
|:-----|:-----|:-----|
| 首屏名字 | 整体淡入 | 三个字逐字从上方轻轻落下，各带微弱旋转 + 0.2s 间隔（像纸片飘落） |
| 区域标题 | 纯文字 | 标题下方加一条手绘感的**装饰线**——不规则细线，像用铅笔随手画的 |
| 页脚收尾语 | 纯文字 | 斜体衬线 + 左侧极淡竖线，做出"引文"质感 |
| 文章标题 | 衬线体 | 衬线体 + 微弱的 `letter-spacing` 过渡动画（hover 时字距微微展开） |

**逐字动画技术**：GSAP `stagger` 实现，每个字独立 `<span>` 或使用 `SplitText` 插件。

**手绘装饰线**：SVG 路径，不规则曲线，颜色 `var(--text-muted)`，opacity 0.4。

---

#### D2. 材质层深化

| 区域 | 纹理强度 | 描述 |
|:-----|:-------:|:-----|
| 首屏 | ⭐⭐⭐ 最浓 | 旧书扉页感 |
| 关于区 | ⭐⭐ | 新纸，极淡纹理 |
| 思考区 | ⭐⭐ | 新纸，极淡纹理 |
| 手记区 | ⭐⭐ | 新纸，极淡纹理 |
| 页脚 | ⭐⭐⭐ 中等 | 毛边纸，稍粗糙 |
| 卡片 | ⭐ 独立微纹理 | 手工卡片纸，独立于背景 |

**技术**：CSS `background-image` 多层叠加，不同区域通过不同的 `radial-gradient` + `repeating-linear-gradient` 参数控制纹理质感。

---

#### D3. 色彩生命力

| 效果 | 实现 |
|:-----|:-----|
| 首屏光晕随时间流转 | 陶土色 ↔ 琥珀色缓慢循环，周期约 20 分钟 |
| 点缀色呼吸 | `--accent` 的饱和度有极微弱的波动（CSS `filter: saturate()` 无效于自定义属性，用 JS 定时微调） |
| 链接 hover 水彩晕开 | hover 时从当前颜色过渡到目标颜色，使用 `cubic-bezier` 模拟晕开效果，0.6s |

---

### 模块 E：时间与氛围系统 ⭐

#### E1. 全天候氛围

全页面色调随时间微调。不是突然变色，而是几分钟内渐变完成。

| 时段 | 底色偏移 | 光晕颜色 | 粒子颜色 |
|:-----|:-----|:-----|:-----|
| 清晨 6:00-9:00 | `#f7f5f2` 偏冷灰白 | 淡蓝灰 `rgba(160,170,185,0.06)` | 冷白 |
| 上午 9:00-12:00 | `#faf7f2` 清爽米白 | 暖白 `rgba(200,185,160,0.06)` | 淡金 |
| 午后 12:00-18:00 | `#faf5ec` 温暖奶油 | 琥珀 `rgba(212,168,83,0.08)` | 暖金 |
| 傍晚 18:00-22:00 | `#f7f0e5` 偏暖橘灰 | 陶土橙 `rgba(193,125,90,0.09)` | 暖橙 |
| 深夜 22:00-6:00 | `#f5ede2` 深暖灰 | 暗琥珀 `rgba(170,110,75,0.08)` | 微蓝 |

**技术**：JS 定时器每 30 秒检测一次当前时段，将对应色值写入 CSS 自定义属性（`--bg`、`--bg-warm` 等），使用 `transition` 实现平滑渐变。

---

## 五大模块优先级总览

| 优先级 | 模块 | 对"生机活力"的贡献 | 你的素材投入 | 预估工作量 |
|:--:|:-----|:-----|:-----|:--:|
| ⭐⭐⭐ | **A. 数据可视化** | 核心亮点 | 确认各标签视觉风格偏好 | 大 |
| ⭐⭐⭐ | **B. 生命律动** | 全局氛围升级 | 植物 SVG 素材收集/确认 | 中 |
| ⭐⭐ | **C. 游戏与彩蛋** | 惊喜和可玩性 | 无额外内容 | 中 |
| ⭐⭐ | **D. 深度视觉** | 精致感大幅提升 | 手绘线条风格确认 | 中 |
| ⭐ | **E. 时间氛围** | 锦上添花 | 无额外内容 | 小 |

---

## 实现顺序建议

```
第一阶段：A + B（数据可视化 + 生命律动）
    ↓  这两个模块对"生机活力"贡献最大
第二阶段：D（深度视觉）
    ↓  在 A+B 的基础上打磨视觉细节
第三阶段：C + E（游戏彩蛋 + 时间氛围）
    ↓  最后的惊喜涂层
```

---

## 待收集素材清单

### 美术素材

- [ ] 植物 SVG：小盆栽、藤蔓、树苗、小叶子 — 极简线条风格，各 1 个
- [ ] 手绘装饰线 SVG — 区域标题下方的不规则细线
- [ ] 像素小角色 spritesheet — 用于游戏标签面板（4帧走动循环，约 16×16 像素风格）
- [ ] 电影票纹理参考 — 确定票根视觉风格

### 内容素材

- [ ] 文章占位内容（6 篇，可先 AI 生成占位）
- [ ] 手记区更多作品图片（可选，持续添加）

---

> 📋 **文档版本**：v2.0-draft
> 📅 **创建日期**：2026年6月
> 🎨 **设计方向**：温润材质 × 锐利工艺 × 生命律动
