# 个人主页优化方案

> 日期：2026年6月
> 状态：讨论中，待实现

---

## 一、体验层优化

### ① 思考区手机体验

**问题**：640px 以下，ScrollTrigger 仍会 pin 住区域做横向翻阅。手机屏幕窄、手指天然上下滑，体验不佳。

**方案**：手机端（≤640px）跳过横向翻阅，降级为正常竖直排列。

| 设备 | 浏览方式 | 卡片动画 | 导航指示 |
|:-----|:---------|:---------|:---------|
| 电脑/平板 (>640px) | 横向翻阅（招牌） | 横向滑动 + 聚焦效果 | 圆点 + 箭头 + 计数器 |
| 手机 (≤640px) | 竖直排列 | 逐个淡入上浮 | 无 |

实现：JS 中 `createThoughtScroll()` 检测 `window.innerWidth < 640` 时跳过 pin/scrub 创建。

---

### ② 区域过渡衔接

**问题**：当前区域各自独立淡入，上一个区域完全消失后下一个才出现，中间有"空白感"，缺乏章节节奏。

**方案 A：重叠淡入**

下一个区域提前 100~150px 开始淡入，两个区域在视觉上有一段重叠。ScrollTrigger 的 `start` 位置从 `top bottom-=100px` 调整为 `top bottom-=200px`。

**方案 B：章节分隔符**

两个区域之间加极简的视觉分隔，如居中的 `· · ·` 三个淡点，随滚动经过时微亮后消失。给访客潜意识的"章末"信号。

**方案 C：滚动微节奏**

思考区 pin+scrub 已自带节奏变化（滚很多走很少），其他区域保持顺畅即可，不与思考区抢戏。

---

### ③ 回到顶部体验

**问题**：当前 `scrollTo(0,0)` 机械匀速，缺乏温度和仪式感。

**方案：快速"倒带"效果**

| 阶段 | 时间 | 画面 |
|:-----|:-----|:-----|
| 停顿 | ~150ms | 停留在页脚，收尾语微亮 |
| 加速上升 | ~400ms | 快速向上，路过各区以微弱淡入闪过 |
| 减速到站 | ~400ms | 接近首屏时慢下来，名字微微做一个呼吸动画（scale 微变，约 0.5s） |

总计约 1 秒，先快后慢，不耽误事但有呼吸感。

---

### ④ 手记区筛选体验

**问题**：手机上筛选后，匹配卡片在页面下方，访客看到空白以为没内容。电脑上同样存在"结果被挤出视口"的问题。

**方案 A：筛选后自动滚动**

点击筛选 → 卡片消失/出现 → 页面平滑滚动到第一个匹配卡片的位置。

**方案 B：结果计数提示**

筛选条旁边短暂浮现 `共 X 条` 文字，1.5 秒后淡去。让人心里有数。

**方案 C：筛选条悬浮**

手机上筛选条始终悬浮在手记区顶部，不随滚动消失，方便随时切换分类。

---

## 二、视觉层优化

### ⑤ 首屏背景层次

**现在**：只有一层光晕跟随鼠标，略显单薄。

**方案：纸纹理 + 四角暗角**

- **纸纹理**：极淡的纸张纤维肌理叠加在背景上，像旧书扉页。不仔细看看不出来，但潜意识觉得"暖"
- **四角暗角**：首屏四角有极其微弱的暗角（vignette），把视线自然聚拢到中间的名字上

不添加颗粒感（避免显"脏"）。纹理和暗角都用 CSS 实现（`background-image` + `radial-gradient`），无需额外图片文件。

---

### ⑥ 导航栏当前项高亮

**现在**：四个导航词在哪个区域都一样颜色。

**方案**：当前所在区域对应的导航词变为陶土色（`var(--accent)`），其他保持灰色。复用已有的侧边导航点判断逻辑，额外给 `.nav-link` 添加 `active` 类即可。

---

### ⑦ 自定义光标

**现在**：默认系统箭头。

**方案**：光标改为小圆点（直径 ~12px），平时半透明陶土色，悬停卡片/链接时放大到 ~24px 并变实心。

**重要**：给访客选择权——在页脚提供极小的开关（`光标效果：○ 开 / ○ 关`），默认开启，点击可关闭。关闭后恢复系统光标。手机上不生效，无需适配。

---

### ⑧ 思考区卡片"聚光灯"

**现在**：所有卡片一样大小、一样亮。

**方案**：根据卡片距离视口中心的偏移量，动态调整：

| 位置 | 缩放 | 透明度 | 阴影 |
|:-----|:---:|:-----:|:---:|
| 正中间 | `1` | `1` | 正常 |
| 往两边偏移 | `0.95` | `0.7` | 减弱 |

横向翻阅时平滑过渡——卡片滑到中间"点亮"，滑出中间"暗下去"。书架上有一束聚光灯的感觉。

---

## 三、功能层优化

### ⑨ 文章详情弹出层

**现在**："阅读全文 →"是空链接，点了没反应。

**方案 A：弹出浮层**

点击文章卡片 → 当前页面弹出一个浮层，文章内容在其中展示。看完关闭，不离开主页。

**当前处理**：文章内容尚未准备好，点击后显示占位提示（如"🚧 正在施工 ing……"），后续内容到位后再接入。如果以后需要跳转到独立页面，也可以在卡片中增加链接，浮层保留亦可共存。

---

### ⑩ 手记区灯箱预览

**现在**：手记卡片只能看缩略图，不能放大。

**方案**：点击手记卡片 → 图片在全屏半透明深色背景下放大展示 → 点击背景或关闭按钮退出。暂做极简版（单图放大），左右切换后续可选加。

---

### ⑪ 公共留言墙

**用户期望**：留言不发到邮箱，而是像便签一样贴到页面上，像咖啡馆的留言墙——所有人写的便签都公开可见。

**方案**：在页脚上方（或联系区与页脚之间）增加一块公共留言墙区域。

| 特性 | 说明 |
|:-----|:-----|
| **留言形式** | 像便签纸贴在墙上，有轻微随机旋转角度（±3°），模拟真实便签 |
| **内容** | 昵称（选填）+ 一句话留言 + 时间戳 |
| **可见性** | 公开，所有人可见。亲友来了可以留"来过啦"、"主页很漂亮" |
| **删除** | 便签主人可手动删除（通过留言时生成的密钥确认身份）。主页主人（你）也可手动清除任何不当便签 |
| **存储** | Firebase Realtime Database（Google 免费额度，1GB/10GB月流量，完全够用） |
| **审核** | 信任模式，不设审核。亲友圈流量小，配合手动删除即可 |
| **位置** | 联系区与页脚之间——"想和我聊聊？"的延续 |
| **氛围** | 配合页脚温润调性，留言墙作为页面上"最有人情味的角落" |

---

### ⑫ 交互组件提示

**问题**：部分可交互组件没有提示，访客不知道可以点击。比如关于区的兴趣标签、页脚的音乐播放器音符。

**方案**：为可交互但不够明显的组件添加微提示。分为两类：

| 组件 | 提示方式 |
|:-----|:---------|
| 兴趣标签 | 首次加载后标签旁短暂浮现轻提示（如 `戳我看看`，2~3 秒后消失）；后续悬停时有 cursor:pointer + 微动效暗示可点击 |
| 隐藏歌单 ♪ | 音符旁偶尔浮现极小文字 `♪ 听歌？`；悬停时微亮；首次访问后记录到 localStorage，后续不再提示 |
| 思考区卡片 | 已有"阅读全文 →"文字引导，卡片悬停有抬起效果，够明显 |
| 页脚隐藏一句话 | 故意不加提示（彩蛋本质），保持被发现时的惊喜感 |
| 手记卡片 | 悬停有抬起 + 放大，但首次访问可在首张卡片上浮一小行提示 `点击查看大图` |

原则：**提示温柔，不喧宾夺主。** 每种提示出现一次后即消失，不反复骚扰。

---

### ⑬ 联系区图标行为修正

**现有问题**：
- QQ 音乐图标点击跳转 `#`（无效，表现为主页跳转到顶部，体验不好）
- 邮箱图标使用 `mailto:` 链接，会打开系统邮件客户端，不是所有人都配置了

**方案**：

| 图标 | 当前行为 | 改为 |
|:-----|:---------|:-----|
| 邮箱 | `mailto:` 打开邮件客户端 | 点击弹出一行地址 `m17385892518@163.com` + `已复制` 提示，自动复制到剪贴板 |
| B 站 | 跳转 B 站空间 | 保持，这是外链 |
| QQ 音乐 | 跳转 `#`（无效） | 点击 → 页面平滑滚动到页脚隐藏歌单 → 音符 ♪ 微微亮一下 + 浮现提示"在这里 ↓" |
