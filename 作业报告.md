# 作业三：Vibe Coding —「我的数据小馆」个人主页

**胡臻烨** / 智能感知工程 / 2024 级
工具：Claude Code (Anthropic Claude 模型)
公网地址：https://qaq82625.github.io/-v2.0/
GitHub 仓库：https://github.com/QAQ82625/-v2.0

---

## 摘要

本实验使用 AI 编程工具制作了一个包含自我介绍、数据可视化和交互彩蛋的个人主页，部署于 GitHub Pages。实验分为两个阶段：第一阶段使用 Codex 采用"逐步构建"策略（先搭骨架→逐模块添加→逐模块调效果），因骨架美观度不足、沟通成本随模块递增、设计一致性失控而放弃；第二阶段改用 Claude Code 采用"设计优先"策略（先与 AI 深度讨论设计计划→汇总为完整设计文档→再执行代码实现），成功打造了以"温润材质×锐利工艺×生命律动"为调性的完整主页，包含四个主题数据可视化面板、横向翻阅招牌交互、三重页脚彩蛋系统和隐藏贪吃蛇游戏。本报告通过两种策略的对比，论证了 Vibe Coding 中"先设计再编码"策略对非技术背景用户的优越性。

---

## 一、实验背景

### 1.1 作业要求概述

课程作业三要求使用 Vibe Coding（AI 编程工具）制作一个个人主页。主页需包含三个必备区域：①自我介绍区——昵称/专业/年级 + 3-5 句有特色的简介 + 一张代表图；②数据可视化区——选一份关于自己的真实小数据，用合适的形式可视化（柱状图、词云、地图、卡片墙等不限）；③交互彩蛋——至少一个能通过"点/悬停/滑动"触发的交互设计。评分从完整性（25分）、个性化（25分）、视觉与创意（20分）、Prompt 工程（20分）、反思深度（10分）五个维度评估，部署公网可获最多 5 分加分。

### 1.2 个人挑战

我没有编程基础，对 HTML/CSS/JavaScript 语法完全不了解。同时我的设计能力有限——我能判断一个设计"好不好看"，但如果让我从零开始做设计决策（配色、布局、动效规格），我缺乏专业参考框架。这两点让我在 Vibe Coding 中面临双重挑战：既不能自己写代码，也做不了精细的视觉决策。

### 1.3 实验目标

本实验的核心问题是：**非技术背景用户如何在 Vibe Coding 中高效驾驭 AI，完成一个兼具功能完整性和视觉品质的个人主页？** 为了回答这个问题，我设计了两种不同的 Vibe Coding 策略进行对照：先用 Codex 尝试"逐步构建"（先骨架→逐模块），后用 Claude Code 尝试"设计优先"（先设计→再实现），对比两种策略在成品质量、过程可控性和效率上的差异。


## 二、第一版：Codex 逐步构建的尝试与失败

### 2.1 工作流程

在第一版中，我使用 Codex，采用的策略是"逐步构建"：先让 AI 搭建一个基本的页面骨架（HTML 结构+基础 CSS 样式），骨架跑通后，再一个模块一个模块地添加内容（先加自我介绍区，再加数据展示区，最后加彩蛋）。每添加一个模块，都要通过多轮对话调整样式和动画效果。

整个流程可以概括为：**搭骨架 → 加模块 A → 调 A 的效果 → 加模块 B → 调 B 的效果 → 加模块 C → … → 发现前面不协调 → 返工调整 → …**

### 2.2 成品展示

> 📸 [此处插入截图 F1：Codex 第一版首页截图]
> *图 F1：Codex 第一版首屏效果*
>
> 📸 [此处插入截图 F2：Codex 第一版内容模块截图]
> *图 F2：Codex 第一版内容区域效果*

第一版最终的效果较为基础：布局大致完整，但色彩选择和字体搭配缺乏统一的设计语言，各个模块的视觉风格不一致，整体观感偏向"模板化"——能看出来是一个网页，但很难记住是谁的网页。

### 2.3 失败原因分析

经过复盘，第一版失败主要有三个核心原因：

**第一，骨架美观度有限，后续修改成本高。** AI 生成的初始骨架往往是最"安全"的默认布局——居中排列、标准间距、基础颜色。从一个不够美观的骨架出发，每改一处样式都可能影响其他区域的布局。修改成本不亚于重新设计，但已经投入的时间让人"舍不得"推倒重来，陷入"越改越累"的循环。

**第二，沟通成本随模块数量递增，且高度依赖 Prompt 精准度。** 每个模块的视觉效果都需要用自然语言精确描述。描述稍有偏差，实际效果与设想就可能差距很大。例如"做一个好看的卡片"和"做一个圆角 12px、暖白底色、悬停时抬升 4px 并加深阴影的卡片"产生的结果完全不同——但初学者往往只知道"不好看"，说不出"差在哪里"。随着模块增多，这种"描述偏差"不断累积，越往后做越失控。

**第三，设计一致性随模块加入而逐渐崩溃。** 由于没有全局设计文档作为参照，每个新模块的样式都是独立通过 Prompt 调整的。当添加到第三个、第四个模块时，回头看第一个模块已经觉得风格不搭了。颜色不统一、间距不一致、动画风格各异——每个模块单独看勉强能看，但放在一起不像同一个网站。

### 2.4 教训

> 代码可以一行行改，但设计方向一旦偏了，修改成本随代码量指数增长。在 Vibe Coding 中，"先写代码再改设计"是低效路径——设计决策应该在代码之前完成，而不是在代码中摸索。


## 三、第二版：Claude Code 设计优先的完整实现

### 3.1 设计策略

基于第一版的教训，第二版我改用 Claude Code，并采用了完全不同的策略：**先设计再实现**。

具体流程是：（1）先与 AI 讨论设计定调——用自然语言对齐"想要什么感觉"（温润×锐气）；（2）规划页面骨架——确定六大区域、叙事顺序和浏览节奏；（3）逐区域细化——每个区域的视觉元素、交互方式、动效规格全部在讨论中确定；（4）汇总为完整设计文档（约 700 行 Markdown），作为后续实现的"唯一真相来源"；（5）最后才开始写代码，一切以设计文档为准。

这个策略的核心思想是：**AI 的设计能力弥补我的设计短板，我作为"决策者"来判断好不好。**"设计"对我来说很难——我不知道什么是好的配色、什么是恰当的动效。但"判断"对我来说相对容易——我能看出一个方案好不好看、合不合理。所以策略的关键转变是：让 AI 生成多个设计方案 → 我判断选哪个 → AI 细化 → 我确认 → 写入设计文档。在整个过程中，我的角色从"设计者"变为"设计评审"。

### 3.2 成品展示

> 📸 [此处插入截图 1：首屏全貌]
> *图 1：首屏 —— 名字逐字飘落动画、菱形装饰线、粒子漂浮、光晕跟随鼠标、2026年份印章*

> 📸 [此处插入截图 2：关于区]
> *图 2：关于区 —— 头像双层边框、自我介绍、专业年级、植物盆栽装饰、四枚兴趣标签*

> 📸 [此处插入截图 3：像素游戏机数据面板]
> *图 3：游戏标签点击展开 —— 复古卡带插槽风格面板 + 像素精灵四帧走动动画*

> 📸 [此处插入截图 4：环形唱片数据面板]
> *图 4：音乐标签点击展开 —— 黑胶唱片旋转（8s一圈）+ 12条声波形随机跳动*

> 📸 [此处插入截图 5：电影票根数据面板]
> *图 5：影视标签点击展开 —— 竖排票根 + 锯齿撕边 + 手写体片名 + 印章风格标签*

> 📸 [此处插入截图 6：CRT 小电视数据面板]
> *图 6：动漫标签点击展开 —— 扫描线效果 + 弹幕漂移动画 + 星星闪烁 + 旋钮装饰*

> 📸 [此处插入截图 7：思考区横向翻阅]
> *图 7：思考区招牌交互 —— 垂直滚动转为水平位移、卡片居中、聚光灯效果、圆点导航+计数器*

> 📸 [此处插入截图 8：手记区瀑布流]
> *图 8：手记区 —— 瀑布流网格 + 植物藤蔓装饰 + 四角几何边框 + 卡片折角 + 背景斜线纹理*

> 📸 [此处插入截图 9：联系区与留言墙]
> *图 9：联系区双层旋转虚线圆环 + 留言墙多色便签 + 横线纸输入框*

> 📸 [此处插入截图 10：页脚与隐藏贪吃蛇]
> *图 10：页脚彩蛋区 —— 时间问候 + 隐藏歌单 + 成就面板 + 隐藏贪吃蛇游戏画面*

### 3.3 成品详细说明

以下对成品每个区域的设计意图、交互触发方式和优化历程进行逐项说明。

#### 3.3.1 全局导航元素

| 元素 | 触发方式 | 设计意图 |
|:-----|:-----|:-----|
| **顶部导航栏** | 首屏时透明（融入背景），滚出首屏后毛玻璃背景淡入（ScrollTrigger 检测 `#hero` 离开视口） | 首屏沉浸体验→后续区域功能性导航，平滑过渡 |
| **导航项高亮** | 滚动时当前区域的导航词自动变为陶土色（`var(--accent)`），下方有滑动下划线自动跟随当前项 | 让访客时刻清楚"我在哪" |
| **侧边圆点** | 页面右侧 5 个圆点对应 5 个区域，亮着的表示当前位置；点击圆点平滑滚动到对应区域 | 辅助导航，不显眼但有用 |
| **章节分隔符** | 每个区域之间 `—— ◇ ——`（水平线 + 中心菱形，菱形每 3s 脉动闪烁），滚动经过时 IntersectionObserver 触发淡入 | 像书页的章节标记，制造"翻页"仪式感 |
| **边缘竖线** | 页面两侧距边缘 2.8rem 各一条固定竖线，陶土色，上下渐隐 | 像笔记本的中缝参考线，给页面安静的"画框" |
| **回到顶部** | 页脚圆形按钮（40px），外围有 SVG 环形进度条实时反映已滚动百分比 | 不只是"回到顶"，还让访客知道"读了多久" |
| **自定义光标** | 页脚开关控制：`光标效果：开/关`；开启后系统箭头变为 14px 陶土色半透明圆点，悬停可交互元素时放大至 28px 实心；晕轮设备不启用 | 给访客选择权，兼顾设计感和实用性 |

#### 3.3.2 首屏

| 元素 | 触发方式 | 实现技术 |
|:-----|:-----|:-----|
| **名字逐字飘落** | 页面加载后自动播放。"胡""臻""烨"三字各为独立 `<span>`，以 0.18s stagger 从上方飘落，带 `back.out(1.5)` 弹性缓动和微弱旋转 | GSAP timeline + stagger |
| **菱形装饰线** | 名字上方和下方各一条水平渐变线 + 中心旋转 45° 菱形，菱形 3.8s 脉动闪烁；随名字入场动画一同淡入 | CSS `linear-gradient` + `@keyframes diamondGlint` |
| **粒子生态系统** | 14 个 Canvas 粒子持续缓慢漂移；鼠标在首屏移动时粒子轻微避让（排斥力），离开后慢慢聚回；粒子透明度 3-5s 周期脉动 | Canvas + requestAnimationFrame |
| **光晕跟随** | 鼠标在首屏移动时，600px 暖色光晕以阻尼缓动跟随（阻尼系数 0.05），25s 周期在陶土色↔琥珀色之间流转 | requestAnimationFrame + CSS `radial-gradient` |
| **2026 年份章** | 首屏右下角 JS 动态注入，48px 圆形印章样式 | JS `Date().getFullYear()` |
| **日付贴纸** | 首屏右上角 54px 圆形贴纸，JS 注入当前月/日，2° 倾斜 | JS 动态生成 |

#### 3.3.3 关于区

| 交互 | 触发方式 | 优化历程 |
|:-----|:-----|:-----|
| **头像双层边框** | 静态显示——内圈苔藓绿实线 + 外圈 box-shadow 光晕；hover 时光晕扩散 | 初版为虚线圆→改为双层圆环+光晕，增强"靶心/唱片纹路"感 |
| **兴趣标签悬停** | 鼠标划过标签时从灰色变为陶土色，0.5s 后消退；未选中标签有微弱的 3.5s 脉动动画 | — |
| **标签提示** | 标签行左侧永久可见的"点开查看 →"文字标签，指向四个兴趣标签 | 初版无提示→加临时浮动提示→改为永久标签，确保访客知道可点击 |
| **数据面板展开** | 点击任意兴趣标签→展开对应主题面板（像素游戏机/环形唱片/电影票根/CRT小电视）；再次点击同一标签→关闭；点击面板外区域→关闭 | 初版为纯文字列表（4行 max-height 展开/收起）→v2.0 全面升级为四个独立主题面板，每个面板有独立的 CSS 布局和 JS 渲染逻辑 |
| **像素精灵动画** | 游戏面板展开后，50×50px 精灵图以 350ms 间隔在 200×50 spritesheet 上逐帧切换（4帧循环），关闭面板停止 | 初版只有一张静态角色图→后续找到 200×50 四帧 spritesheet→用 `object-position` 逐帧偏移实现走动动画 |
| **唱片旋转** | 音乐面板展开后，CSS `conic-gradient` 黑胶唱片以 8s 一圈持续旋转 | — |
| **声波形跳动** | 音乐面板展开后，12 条声波柱每 120ms 随机变化高度 | JS `setInterval` + `Math.random()` |
| **弹幕漂移** | 动漫面板展开后，"数不清咯……"以 CSS `@keyframes bulletDrift` 实现 8s 周期从右到左循环漂移 | — |
| **星星闪烁** | 动漫面板中"★ 都给我去看！★"以 1.5s 周期脉动闪烁 | CSS `@keyframes starBlink` |
| **电影票撕边** | 影视面板中票根右侧锯齿状裁剪 | CSS `border-radius: 50%` 模拟的圆形缺口叠加 |

#### 3.3.4 思考区（招牌交互）

| 交互 | 触发方式 | 实现技术 | 优化历程 |
|:-----|:-----|:-----|:-----|
| **横向翻阅** | 向下滚动进入思考区后，ScrollTrigger pin 住整个区域，访客继续向下滚但视觉上卡片从左向右横向滑过。6 张翻完后恢复垂直滚动 | GSAP ScrollTrigger `pin: true, scrub: 1` + timeline 驱动 `translateX` | 初版完整实现 → 增加聚光灯效果 → **修正 AI 理解偏差**（AI 把"卡片居中"理解成"标题居中"，纠正后卡片在标题下方 flexbox 垂直居中） |
| **聚光灯效果** | 当前居中卡片 `scale(1) opacity(1)`，往两边偏移的卡片 `scale(0.94) opacity(0.55)`，过渡平滑 | JS `updateSpotlight()` 根据 ScrollTrigger progress 计算每张卡片距中心偏移量 | — |
| **圆点导航** | 卡片下方 6 个圆点，当前卡片对应圆点高亮（1.5× 放大）；点击任意圆点→GSAP 控制 ScrollTrigger progress 跳转 | `scrollToCard(index)` 计算对应 progress 值 | — |
| **箭头导航** | 左右箭头，点击切换卡片；第一张时左箭头隐藏，最后一张时右箭头隐藏 | `classList.toggle('hidden')` | — |
| **计数器** | 右下角显示 `1 / 6` ~ `6 / 6` | — |
| **卡片悬停** | hover 时卡片抬升 4px + 阴影加深 + 封面 `scale(1.04)` + 字距微展 `letter-spacing: 0.04em` + 对角线柔光扫过（1.2s ease-out） | CSS `transition` + `::after` 伪元素光照 | 光照初版默认显示（亮度 0.25）→太刺眼→改为仅 hover 触发，亮度降至 0.14 |
| **卡片折角** | 右上角固定几何折角（陶土色三角），4.5s 微弱闪烁，hover 加深 | CSS `::before` 伪元素 linear-gradient 三角 | — |
| **封面视差** | 封面图比文字移动稍慢 | GSAP `scrollTrigger scrub: 0.5` | — |
| **手机降级** | 640px 以下跳过 pin/scrub 创建，卡片竖直排列，逐个淡入上浮；导航指示器隐藏 | `createThoughtScroll()` 函数开头 `if (window.innerWidth < 640) return` | **这是从"手机无法使用"到"正常浏览"的关键优化**。初版未处理→手机体验极差→加入降级逻辑 |

#### 3.3.5 手记区

| 交互 | 触发方式 | 实现技术 | 优化历程 |
|:-----|:-----|:-----|:-----|
| **瀑布流布局** | CSS `columns` 自动分列：桌面 3 列 / 平板 2 列 / 手机 1 列，卡片高度随图片自然变化 | CSS `columns` + `break-inside: avoid` | — |
| **分类筛选** | 点击顶部筛选按钮（全部/生活/游戏/学习/素材），不匹配卡片 GSAP 淡出缩小→200ms 后匹配卡片重新排列淡入；筛选条旁显示"共 X 条"1.8s 后淡去 | GSAP `to()`/`fromTo()` + `classList.toggle('hidden')` | 初版仅有简单显隐→增加：**(a) 排序重排动画**（卡片平滑移动到新位置）、**(b) 计数提示、**(c) 筛选后自动滚动到第一个匹配卡片、**(d) 手机筛选条吸顶悬浮** |
| **图片预览（灯箱）** | 点击任意手记卡片→图片在全屏深色背景（88%透明度）下放大展示（88vw×85vh）；点击背景/关闭按钮/ESC 键退出 | CSS `position:fixed` 遮罩 + JS `classList.toggle('open')` | **关键优化：从"无法预览"到"可以预览"**。初版手记卡片只有缩略图，没有任何放大查看途径（手机瀑布流单列时图片更小，完全看不清细节）。灯箱功能使每张手记图片都可以被仔细观看 |
| **筛选条悬浮** | 手机上（≤640px）筛选条始终吸顶悬浮在手记区顶部，毛玻璃背景，不随滚动消失 | CSS `position: sticky` + `backdrop-filter` | 初版筛选条随页面滚走→切换到新分类后看不到结果→加入吸顶 |
| **卡片视差** | 滚动时卡片封面图以比文字慢约 20% 的速度跟随 | GSAP `scrollTrigger scrub: 0.5` | — |
| **渐变遮罩** | hover 时卡片图片底部浮现 35% 高度暗色渐变遮罩（`linear-gradient`），像杂志封面效果 | CSS `::after` 伪元素 | — |
| **卡片光照** | hover 时图片表面有对角线柔光斜向扫过（1.2s ease-out） | CSS `::before` 伪元素 + `background-position` 动画 | 与渐变遮罩共用伪元素→冲突修复 |
| **分类微配色** | 生活卡片底 `#fdfaf6`（暖黄）、游戏 `#faf9f6`（冷灰）、学习 `#fdfcfa`（米白）、素材 `#f9faf7`（淡绿） | CSS 属性选择器 `[data-category="xx"]` | 细微不可查但统一中有层次 |

#### 3.3.6 联系区

| 交互 | 触发方式 | 优化历程 |
|:-----|:-----|:-----|
| **双层旋转圆环** | 社交图标外围双层虚线圆环——内圈顺时针 40s、外圈逆时针 65s 持续旋转 | CSS `@keyframes ringSpin` + `border: dashed` |
| **邮箱图标** | 点击→自动复制邮箱地址到剪贴板（`navigator.clipboard.writeText`）→底部弹出 Toast"邮箱地址已复制：m17385892518@163.com"，2.2s 后消失 | **初版用 `mailto:` 链接**→会打开系统邮件客户端，很多访客的电脑未配置邮箱→改为剪贴板复制 |
| **B 站图标** | 点击→新标签页打开 B 站空间 `https://space.bilibili.com/351637040` | — |
| **QQ 音乐图标** | 点击→页面平滑滚动到页脚隐藏歌单→自动展开歌单→音符 ♪ 做 GSAP 呼吸动画 + Toast"在这里 ↓ 听听歌吧" | **初版为无效的 `#` 空链接**（滚动到页面顶部，体验很差，而且 QQ 音乐图标无法正常使用）→改为引导至隐藏歌单 |
| **图标悬停** | 悬停时图标放大 1.1× + 变为平台主题色 + 下方浮现提示词（"写信给我"/"看看视频"/"听歌吗"） | 纯 CSS `transition` + `.social-label` |

#### 3.3.7 留言墙

| 交互 | 触发方式 | 优化历程 |
|:-----|:-----|:-----|
| **留言输入** | 输入框有横线纸纹理（23px 行距 `repeating-linear-gradient`），输入昵称（选填，maxlength 20）+ 留言内容（必填，maxlength 200）后点"贴上便签"提交 | **初版为纯白输入框**→加入横线纸纹理增强手帐感 |
| **便签展示** | 新便签以 back.out 弹性动画弹出（`scale 0.5→1`），每条 ±2.5° 随机旋转模拟真实便签，颜色轮换（淡黄/淡粉/淡蓝/淡绿 4 色） | **初版所有便签同一淡黄色**→改为 4 色轮流分配（存入 message 的 colorIndex 字段） |
| **便签删除** | 鼠标悬停便签→右上角出现 ✕ 按钮（`opacity 0→1`），点击删除→localStorage 更新→重新渲染 | — |
| **按钮质感** | "贴上便签"按钮使用渐变仿图钉质感（`linear-gradient(180deg, #d4a060→#b8743a)`）+ 内阴影 + 按下时 translateY(1px) 位移反馈 | 通用平色按钮→仿图钉质感 |
| **空状态** | 无便签时显示"还没有便签，来做第一个留言的人吧 ✨" | — |
| **碎纸片装饰** | 留言墙背景中 2 个随机旋转（±7°）长方形碎纸片 | CSS `transform: rotate()` + `paper-scrap` 类 |

#### 3.3.8 页脚（彩蛋系统）

彩蛋按发现难度分为四层：

| 层级 | 彩蛋 | 触发方式 | 难度 |
|:--:|:-----|:-----|:--:|
| L1 | **时间问候** | 自动显示，根据浏览器时间 (`new Date().getHours()`) 切换 4 条问候语：6-9 点"早…"、9-18 点"白天的时光…"、18-22 点"傍晚好…"、22-6 点"这么晚了…" | ★ 自动 |
| L2 | **隐藏歌单** | 点击 ♪ 音符图标→展开迷你播放器（2 首：屑屑 / 不眠之夜）→点 ▶ 播放；再次点击 ♪ 收起；歌曲播放完毕自动解锁"音乐鉴赏家"成就 | ★★ 需留心 |
| L2 | **彩蛋成就** | 点击 👁️ 图标→展开 6 个圆形徽章面板；解锁条件：时间旅人（四个时段各访问一次）/ 音乐鉴赏家（听完一首歌）/ 秘密读者（发现隐藏一句话）/ 留言达人（写一条便签）/ 游戏高手（贪吃蛇≥20分）/ 深夜访客（0:00-4:00 访问）；解锁时 14 个彩色纸屑粒子从屏幕中央散开 | ★★ 需发现入口 |
| L3 | **隐藏一句话** | 鼠标在版权行 `© 2026 胡臻烨 · 差不多行了` 上停留 1.5s → 文字 GSAP 淡化→重组为悄悄话"你居然连这里都看了——那我们应该是朋友了。"→ 移开后恢复 | ★★★ 需耐心 |
| L4 | **贪吃蛇游戏** | 点击页脚 `~` 装饰符 5 次（1.2 秒内）→ 全屏暗色遮罩弹出 Canvas 游戏；蛇身陶土色渐变、食物苔藓绿、网格线极淡；方向键/WASD/触屏滑动控制；死亡后显示"再来一局"+ 最高分（localStorage）；ESC / 点击背景退出 | ★★★★ 需知道"密码" |

#### 3.3.9 关键优化历程汇总

以下 15 个优化点记录了成品从初版到最终状态的重要变化。每一条都对应着"发现问题→分析原因→提出方案→实施"的完整迭代循环：

| # | 优化点 | 改前问题 | 改后方案 |
|:--:|:-----|:-----|:-----|
| 1 | **手机手记图片预览** | 瀑布流只有缩略图，手机上单列时图片更小，无法放大查看 | 点击任意卡片触发灯箱全屏预览（88vw×85vh） |
| 2 | **手机思考区体验** | 横向翻阅在手机上卡顿反直觉，手指天然上下滑却推不动横向内容 | 640px 以下跳过 pin/scrub，降级为竖直排列 |
| 3 | **手机筛选体验** | 筛选后匹配卡片可能在屏幕下方，访客看到空白以为没内容 | 筛选后自动滚动+计数提示+筛选条吸顶悬浮 |
| 4 | **邮箱图标行为** | `mailto:` 链接打开系统邮件客户端，很多电脑未配置 | 点击复制地址到剪贴板 + Toast"已复制"提示 |
| 5 | **QQ 音乐图标行为** | 空链接 `#`，点击跳到页面顶部，没有实际功能 | 平滑滚动到页脚歌单 + 自动展开 + 音符呼吸动画 |
| 6 | **兴趣标签提示** | 没有任何提示，访客不知道标签可以点击打开数据面板 | 左侧永久可见文字"点开查看 →" + 首次加载浮动提示 |
| 7 | **数据面板** | 纯文字列表，`max-height` 展开收起动画 | 四个独立主题面板（像素游戏机/唱片/票根/电视） |
| 8 | **回到顶部按钮** | GSAP ScrollTo 插件未加载，按钮点击无反应 | 改用原生 `window.scrollTo({behavior:'smooth'})` |
| 9 | **联系区图标点击** | 圆环伪元素遮挡点击 | `pointer-events: none` |
| 10 | **卡片光照** | 默认显示，亮度 0.25，卡片持续闪烁刺眼 | 改为仅 hover 触发，亮度降至 0.14 |
| 11 | **便签颜色** | 所有便签同一淡黄色 | 4 色轮流分配（淡黄/淡粉/淡蓝/淡绿） |
| 12 | **植物装饰** | 60-80px，opacity 0.55，缩在角落难以察觉 | 放大到 120-160px，opacity 0.85，移到显眼位置 |
| 13 | **整体视觉** | 留白过多，缺少视觉骨架，装饰语言单调 | 几何线条骨架（菱形/边框/圆环/折角）+ 手帐六套元素 |
| 14 | **导航反馈** | 导航项无当前状态指示 | 滑动下划线 + 陶土色高亮 |
| 15 | **留言输入框** | 纯白背景 | 笔记本横线纸纹理（23px 行距） |


## 四、设计说明

### 4.1 我想呈现什么样的自己

设计之初我给自己定了两个关键词：**"温润"与"锐气"**。

温润落在"材质"上——暖米白纸质感底色、衬线字体标题、充裕留白，像午后书房里摊开的一本手帐。锐气落在"工艺"上——精准的动效、思考区横向翻阅、四种完全不同风格的兴趣数据可视化。这两个看似矛盾的词在设计中形成张力：底色是安静的，但交互是锋利的。

后期加入了第三个关键词：**"生命律动"**。页面不应该是静止的——粒子在漂浮、唱片在旋转、卡片光照在 hover 时轻轻扫过、标题下方有手绘感的装饰线、角落的植物在微风中摇摆。安静但活着，这是我对这个主页最满意的状态。

三个关键词的关系可以总结为：**温润材质 × 锐利工艺 × 生命律动**——温润是基底，锐气是骨骼，律动是呼吸。

与第一版不同的是，第二版的整个设计过程都是在 AI 的辅助下完成的——我没有自己画配色方案，而是通过 Prompt 引导 AI 提出方案，我来判断是否合适。"设计优先"策略让我这个非设计师也能做出有专业感的设计决策。

### 4.2 为什么选这份数据

四个兴趣标签——**游戏、音乐、影视、动漫**——是我每天真实投入时间的事。数据不是编造的："本月已听 241 首""生理年龄减 7 年""数不清咯"，每一条都是真实的日常状态。作业要求中提到"10 条真实有趣的数据，远胜于 100 条编造的数据"，我的这四条兴趣数据虽然量不大，但每一条都带着个人印记。

选择这四个标签还因为它们足够差异化——游戏适合像素风的复古质感，音乐适合黑胶唱片与声波的视听结合，影视适合老电影票根的怀旧叙事，动漫适合 CRT 电视的屏幕美学。四种完全不同的视觉语言，统一在温润的基调下，各自成立又不互相冲突。如果四个标签都用同一种图表样式（比如四个柱状图），信息虽然清晰但毫无个性——而作业的核心要求正是"个性化"。

### 4.3 设计演进路线

主页从零开始，经历了三个大版本和两轮视觉增强：

- **v1.0（基础框架）**：六大区域结构、思考区横向翻阅招牌交互、三重页脚彩蛋（时间问候+隐藏歌单+隐藏悄悄话）。搭建了完整的页面骨架和核心交互逻辑。
- **v1.1（体验与视觉优化）**：加入移动端降级（640px 以下跳过横向翻阅）、区域过渡衔接（重叠淡入+章节分隔符+微节奏）、回到顶部倒带效果、首屏纸纹理与四角暗角、自定义光标（可开关）、思考区卡片聚光灯效果、文章弹出层（施工 ing 占位）、手记灯箱预览、公共留言墙（localStorage 版本）。
- **v2.0（五大模块升级）**：A. 数据可视化——四个兴趣标签主题面板（像素游戏机 / 环形唱片 / 电影票根 / CRT 小电视）；B. 生命律动系统——粒子生态系统 + 植物 SVG 装饰（4个，放大 2.5×）+ 呼吸感动画；C. 游戏与彩蛋——隐藏贪吃蛇（Canvas）+ 彩蛋成就系统（6 个成就 / 纸屑解锁动画）；D. 深度视觉——逐字飘落动画 + 手绘装饰线 + 分级纸纹理（5 级）+ 色彩生命力；E. 时间与氛围——全天候六时段色调渐变（每 30 秒检查）。

随后进行了**两轮视觉增强**——几何线条骨架（边缘竖线、首屏菱形装饰线、四角 L 形边框、标题圆点、章节菱形分隔符、卡片折角、联系区双层旋转圆环、背景微图案）和手帐背景增强（和纸胶带 4 条、印章邮戳 3 枚、手绘线条 4 处、碎纸片 5 片、交互翘角贴纸、日付贴纸与章节页码），以及 12 项细节打磨（卡片光照优化、便签多色、留言横线纸、页脚分组、回顶环形进度、导航滑动下划线等）。


## 五、关键 Prompt 记录

以下精选 8 条决定项目走向的关键 Prompt。完整对话约 200+ 轮交互，如有需要可提供完整记录。

### Prompt ①：确立核心调性

**背景**：AI 首先提出了"温润"的暖色调设计方向（纸张质感、暖米白底色、衬线字体），我认可但觉得还不够——我希望主页不止温暖，还要有设计感和交互感。

**【我的 Prompt】**：温润的同时，我需要网页设计带有一些锐气，比如各种动态效果，让网页显得更加具有设计感和交互感。

**【AI 回应】**：AI 提炼出"温润是材质，锐气是工艺"的核心矛盾——温润落在色彩/字体/留白上，锐气落在视差滚动、光标交互、文字显现动画、悬浮反馈上。制定了动画原则：快入慢出（0.3~0.8s）、克制用量（一页一两个亮点）、每个动效有意义。

**【我的判断】**：这个框架让我立刻理解了设计方向——不是两边各做一点，而是"用温润包裹锐利"。这是整个主页最重要的一条 Prompt，后续所有设计决策都基于此。

---

### Prompt ②：决定招牌交互

**背景**：页面骨架设计时，我觉得文章列表区普通纵向排列太常规，想要一个让人记住的交互。

**【我的 Prompt】**：能不能在某一个板块，将垂直滑动替换为横向滑动，像书页条目一样一页一页略过？

**【AI 回应】**：AI 分析了六个区域各自适合横向滑动的程度，推荐在"思考区（文章）"做横向翻阅——"文章=书页"的隐喻天然契合，比常见的画廊式横向滑动更出乎意料。

**【我的判断】**：这是我认可的第一个重大交互决策。思考区的横向翻阅成为整个主页最核心的交互亮点，使用了 GSAP ScrollTrigger 的 pin+scrub 实现。

---

### Prompt ③：启动数据可视化

**背景**：AI 建议兴趣标签点击展开数据面板，我同意方向，但需要为每个标签确定具体的视觉风格。

**【我的 Prompt】**：游戏标签：像素风；音乐标签：波形动画加环形唱片；影视：电影票；动漫：小电视机。

**【AI 回应】**：AI 为四个兴趣标签分别设计了独立的数据面板主题：像素游戏机（复古卡带插槽+精灵四帧走动动画）、环形唱片（CSS conic-gradient 黑胶旋转+12条随机跳动声波）、电影票根（竖排票根+锯齿撕边+手写体片名）、CRT小电视（扫描线+弹幕漂移+旋钮装饰）。

**【我的判断】**：这个方案完全满足作业的数据可视化要求——数据真实，形式有创意。四种完全不同的主题，比柱状图/饼图有温度得多。

---

### Prompt ④：设计彩蛋系统

**背景**：AI 提出页脚可以加入彩蛋，并给出了多个方向的建议。

**【我的 Prompt】**：认可时间问候+隐藏歌单+隐藏一句话三者组合。按难度分层：时间问候自动显示、隐藏歌单点击触发、隐藏一句话悬停 1.5 秒浮现。

**【AI 回应】**：AI 将三个彩蛋整合进页脚设计，形成清晰的发现难度分层。后续又加入了贪吃蛇（连点 ~ 五次触发）和六个成就徽章。

**【我的判断】**：彩蛋的分层设计体现"温润"——不喧宾夺主，但被有心人发现时会心一笑。

---

### Prompt ⑤：纠正 AI 的理解偏差

**背景**：我要求思考区卡片"放在屏幕中间"，AI 理解成了把"随笔"标题居中固定在屏幕中央，导致标题遮挡了卡片。

**【我的 Prompt】**：不是要随笔两个字放在中间，而是要随笔的卡片放在中间，随笔仍然在顶部。

**【AI 回应】**：AI 立即修正——标题恢复顶部自然定位，卡片 track 容器使用 flexbox 的 align-items: center + min-height 实现卡片在标题下方自动垂直居中。

**【我的判断】**：这是 Vibe Coding 的典型纠错场景——说错了不可怕，说清楚哪里错了才重要。一句精确的否定+重述比重新发需求更高效。

---

### Prompt ⑥：推动视觉升级

**背景**：v2.0 五个模块完成后，功能丰富但视觉空——留白过多，植物小得像灰尘。

**【我的 Prompt】**：网页整体视觉太空了，植物素材太小难以注意。需要更大胆一些，可以大胆使用线条和几何图形，也可以使用更多装饰和动态效果。

**【AI 回应】**：AI 提出了全面的视觉填充方案——植物从 60-80px 放大到 120-160px，新增边缘竖线、首屏菱形装饰线、四角 L 形边框、标题两侧圆点、章节菱形分隔符、卡片折角、联系区双层旋转圆环、背景微图案等十几项几何装饰。

**【我的判断】**：这次升级让页面从"朴素精致"变成"丰富有骨"。几何线条作为视觉骨架，填补空洞但保持了温润——所有装饰都使用低透明度、淡色系。

---

### Prompt ⑦：手帐背景增强

**背景**：视觉填充后结构变好了，但背景还可以更有"手工质感"。

**【我的 Prompt】**：背景能不能在保留手帐感的同时添加更多元素？

**【AI 回应】**：AI 提出六套手帐元素方案：A.和纸胶带斜贴装饰（4条不同样式）；B.印章邮戳（收藏章/星形章/年份章）；C.手绘线条（荧光笔下划线/双线边框/圈注椭圆）；D.碎纸片（5个随机旋转长方形）；E.日付贴纸+章节页码；F.交互翘角贴纸（hover时翘起）。全部用 CSS 实现。

**【我的判断】**：六个方案让页面从"好看的网页"升级为"有手工质感的空间"。胶带和印章是"有人在这个页面上做手帐"的痕迹。

---

### Prompt ⑧：细节打磨

**背景**：所有功能就位后，开始关注实际体验的细节。卡片光泽扫过效果发现后太亮太闪。

**【我的 Prompt】**：卡片光泽扫过效果实际体验比较刺眼，hover 时带上光泽即可。

**【AI 回应】**：AI 将光照从默认显示改为仅 hover 触发，最高亮度从 0.25 降至 0.14，位移范围从 180% 缩至 150%，过渡延长到 1.2s。同时修复了光泽与折角、渐变遮罩共用伪元素的冲突。

**【我的判断】**：这是 Vibe Coding 中"体验验收"的典型过程——功能正确不等于体验好。"刺眼"这个词比"亮度太高"更直观有效。


## 六、踩坑复盘

### 坑 1（方法论之坑）：Codex 第一版逐步构建的失败

**【发生了什么】**：在当前的 Claude Code 版本之前，我首先用 Codex 尝试制作个人主页，采用"逐步构建"策略——先搭骨架 → 一个模块一个模块加 → 逐个调效果。结果骨架不够好看，改起来很痛苦；每个新模块的 Prompt 精准度要求极高，偏差累积后风格越来越不统一；最终第一版被放弃。

**【怎么发现的】**：模块添加到第三个时，回头看第一个模块已经觉得风格不搭了。越往后做越觉得失控——像拼一幅没有参考图的拼图，每一块放下去才发现和前面不匹配。

**【怎么解决的】**：放弃第一版，换用 Claude Code，改用"设计优先"策略：和 AI 先花了大量时间讨论设计方向——从"温润×锐气"的调性，到六个区域的精确设计，到数据可视化的四种主题风格，到彩蛋系统的层次。一切确认后才开始写代码。

**【如果重做会怎么改进】**：Vibe Coding 中"先写代码再改设计"是低效的——代码改一行很容易，但设计方向偏一点，改动成本随着代码量的增加呈指数级增长。**先设计、后编码**——把"写代码前"的讨论当作最重要的投入——是我从这次失败中学到的核心经验。

---

### 坑 2：思考区在手机上的体验

**【发生了什么】**：横向翻阅在桌面端是亮点，但手机上 ScrollTrigger 的 pin 机制让用户"卡"在思考区——手指天然上下滑却推不动横向内容。

**【怎么发现的】**：设计讨论时 AI 主动提到"640px 以下需改善"，但最初版本没有实际处理。

**【怎么解决的】**：在 `createThoughtScroll()` 中加入 `window.innerWidth < 640` 判断，手机端跳过 pin/scrub，卡片降级为竖直排列。

**【如果重做】**：一开始就要求 AI"同时给出移动端和桌面端方案"。

---

### 坑 3：联系区图标无法点击

**【发生了什么】**：新增的双层旋转圆环的 `::before`/`::after` 伪元素没有 `pointer-events: none`，像透明玻璃盖在图标上方，点击无反应。

**【怎么发现的】**：实际点击邮箱/B站/QQ音乐均无响应，排查新元素。

**【怎么解决的】**：给两个伪元素加 `pointer-events: none`。

**【如果重做】**：装饰性伪元素默认加 `pointer-events: none`，养成习惯。

---

### 坑 4：回到顶部按钮失效

**【发生了什么】**：AI 用了 `gsap.to(window, { scrollTo: 0 })` 但项目没引入 GSAP ScrollToPlugin，GSAP 静默忽略调用。

**【怎么发现的】**：点击按钮无反应，控制台无报错。

**【怎么解决的】**：改用原生 `window.scrollTo({ top: 0, behavior: 'smooth' })`。

**【如果重做】**：优先使用浏览器原生 API，避免不必要的插件依赖。

---

### 坑 5：卡片光泽刺眼 + 伪元素冲突

**【发生了什么】**：卡片"光照扫过"默认状态就可见，亮度 0.25，持续闪烁刺眼。同时 `::after` 被光泽、折角、渐变遮罩三个效果争抢。

**【怎么发现的】**：浏览时卡面视觉过于活跃。

**【怎么解决的】**：（1）光照改为仅 hover；（2）亮度降至 0.14；（3）光泽移至子元素伪元素避免冲突。

**【如果重做】**：做视觉特效时先要"低配版"，从低向高调。

---

### 坑 6：AI 理解错"卡片居中"

**【发生了什么】**：我说"卡片放在屏幕中间"，AI 把"随笔"标题固定在屏幕中央（`position: fixed`），遮挡卡片。

**【怎么发现的】**：看到效果明显不对，"随笔"浮在页面上方。

**【怎么解决的】**：精确描述纠正——"不是标题居中，是卡片居中，标题在顶部"。

**【关键经验】**：Vibe Coding 中关键技巧是"否定+精确重述"——不是 A，而是 B。


## 七、Vibe Coding 思考总结

### 7.1 我的角色 vs AI 的角色

在这个项目中，我的角色是"决策者"——决定做什么、什么风格、什么感觉，判断好坏并描述问题。AI 的角色是"执行者"——技术实现、方案生成、快速迭代。整个过程中我做的最多的事不是写代码，而是"看效果 → 说不满意 → 描述哪里不对 → 让 AI 再改"。

### 7.2 两种 Vibe Coding 策略的对比

从 Codex 到 Claude Code，我经历了两种完全不同的 Vibe Coding 策略：

| | 策略 A：逐步构建（Codex 版） | 策略 B：设计优先（Claude Code 版） |
|:-----|:-----|:-----|
| **流程** | 搭骨架 → 加模块 → 调效果 | 讨论设计 → 写设计文档 → 执行实现 |
| **设计一致性** | 模块独立调整，越做越失控 | 设计文档锁定全局方向，不会跑偏 |
| **Prompt 要求** | 每个模块需高度精准的 Prompt 控制效果 | 设计阶段确认方向即可，实现阶段有文档作参照 |
| **修改成本** | 随模块数量指数增长 | 有设计文档作"纠错参照"，修改方向明确 |
| **适合谁** | 有明确设计稿、知道自己要什么的人 | 设计能力有限、需要 AI 辅助决策的人 |
| **结果** | 风格失控，放弃 | 成品远超预期 |

**核心差异**：策略 A 把 AI 当"代码机器"，策略 B 把 AI 当"设计伙伴"。策略 A 中我做的是"设计 + 调试"，策略 B 中我做的是"评审 + 决策"——后者是更适合非专业用户的角色。

### 7.3 有效的 Prompt 策略

从本项目中总结的最有效的四种 Prompt 模式：**比喻法**（"像书页条目一样一页一页略过"——AI 立刻理解）；**否定+精确描述**（"不是 A，而是 B"——立即修正）；**场景化描述**（"像素游戏机面板：复古卡带插槽风格"——比"做好看"有效）；**感官词**（"刺眼"——比"亮度太高"直观）。

### 7.4 结语

Vibe Coding 不是"AI 替我做"，而是"我主导方向 + AI 高效执行"。这个项目最有价值的产出不是代码——而是**"先设计再实现"这套方法论**。从 Codex 的失败到 Claude Code 的成功，我学到的不是某个工具更好用，而是一条普适原则：**在把 Prompt 变成代码之前，先把 Prompt 变成设计**。


## 附录

### A. 技术栈

| 技术 | 用途 |
|:-----|:-----|
| HTML5 | 页面结构 |
| CSS3 | 样式（~3100行）、动画、响应式 |
| JavaScript (Vanilla) | 交互逻辑 |
| GSAP + ScrollTrigger | 高级动画控制（横向翻阅、入场动画、视差） |
| Google Fonts | 字体 |
| GitHub Pages | 公网部署 |

### B. 文件结构

```
├── index.html        ← 页面结构（六大区域）
├── style.css         ← 所有样式（~3100行）
├── script.js         ← 交互逻辑（~1700行）
├── 设计文档.md        ← 完整设计文档（~700行）
├── v2.0设计计划.md    ← 五模块升级计划
├── 优化方案.md        ← 13项优化方案
└── assets/
    ├── 头像/
    ├── 文章封面/      ← 6 张
    ├── 手记区/        ← 生活/游戏/学习/素材
    ├── 植物svg/       ← 8 个
    ├── 音乐素材/      ← 2 首 mp3
    └── 像素小人_50x50像素_四帧/
```

### C. 工具与模型

- 第一版工具：Codex
- 第二版工具：Claude Code (VS Code 插件版)，模型 Claude Opus 4.8
- 交互方式：自然语言中文对话，AI 直接读写本地文件
- 交互轮次：约 200+ 轮
- 版本管理：Git + GitHub

### D. 设计文档

完整设计文档、v2.0 设计计划、优化方案、Prompt 记录材料均随项目文件提交于 Git 仓库。
