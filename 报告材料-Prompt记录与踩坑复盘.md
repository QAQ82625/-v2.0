# 作业三报告材料：Prompt 记录与踩坑复盘

> 胡臻烨 / 智能感知工程 / 2024级  
> 工具：Claude Code (Anthropic Claude 模型)  
> 公网地址：https://qaq82625.github.io/-v2.0/

---

## 一、关键 Prompt 记录

以下精选 8 条决定项目走向的关键 Prompt，完整展示了从设计定调到细节打磨的 Vibe Coding 全过程。

---

### Prompt ①：确立核心调性

**背景**：AI 首先提出了"温润"的暖色调设计方向（纸张质感、暖米白底色、衬线字体），我认可但不满足——我希望这个主页不止是"安静温暖"，还要有"设计感"。

**我的 Prompt**：

> 温润的同时，我希望能让网页设计带有一些锐气，即是网页的设计应当有一些高端的效果（各种动态效果等），让这个网页显得更加具有设计感和交互感。

**AI 回应**：AI 重新梳理了调性，提出"温润是材质，锐气是工艺"的核心矛盾——温润落在色彩/字体/留白上，锐气落在视差滚动、光标交互、文字显现动画、悬浮反馈等高端效果上。并制定了动画设计原则：快入慢出、克制用量、每个动效有意义。

**我的判断**：这个提炼非常精准。"材质×工艺"的矛盾框架让我立刻理解了设计方向——不是两边各做一点，而是用温润包裹锐利。后续所有设计决策都建立在这个基础上。

---

### Prompt ②：决定招牌交互

**背景**：定调之后，AI 给出了六大区域的页面骨架方案。浏览到思考区（文章列表）时，我觉得普通纵向排列太常规了，想要一个能让人记住的交互。

**我的 Prompt**：

> 能不能在某一个板块，将网页浏览的垂直滑动视觉效果替换为横向滑动，即是在网页继续向下滑的过程中，某一个板块视觉上是横向的滑动，像书页条目一样一页一页略过。

**AI 回应**：AI 分析了六个区域各自适合横向滑动的程度，推荐在"思考区（文章）"做横向翻阅——因为"文章=书页"的隐喻最贴切，而且相比常见的作品区横向滑动（画廊），文章区做横向翻阅更出乎意料。AI 还详细描述了视觉效果：垂直滚动被"接住"转为水平位移，卡片从右向左滑过，像一个横排相册。

**我的判断**：我认可了思考区做横向翻阅的方案。这是整个主页最核心的交互特色，后来也成了作业的"招牌动作"。AI 提到了"滚动劫持（Scroll Hijacking）"的概念，并提醒"用得好是亮点，用过度是灾难"——这个判断很专业。

---

### Prompt ③：启动数据可视化

**背景**：AI 建议关于区的兴趣标签点击后展开数据面板。我同意这个方向，但需要确定每个标签的具体视觉风格。

**我的 Prompt**：

> 关于 A（数据可视化），游戏标签：像素风 / 音乐标签：波形动画加环形唱片 / 影视：电影票 / 动漫：小电视机。

**AI 回应**：AI 为四个兴趣标签分别设计了独立的数据面板主题：
- 游戏 → 像素游戏机面板：复古卡带插槽风格 + 精灵 sprite 四帧走动动画
- 音乐 → 环形唱片面板：CSS conic-gradient 黑胶唱片旋转 + 12 条随机声波形
- 影视 → 电影票根面板：竖排票根 + 锯齿撕边 + 手写体片名 + 印章风格标签
- 动漫 → CRT 小电视面板：扫描线效果 + 弹幕漂移动画 + 星星闪烁

每个面板都不仅仅是"把数据换个样式展示"，而是构建了一个微型场景。

**我的判断**：这个可视化方案完全满足作业要求——数据是真实的（听歌量、追番数、游戏时长），形式是有创意的（四个完全不同的主题风格）。比简单的柱状图/饼图有温度得多。

---

### Prompt ④：设计彩蛋系统

**背景**：AI 提出页脚可以加入彩蛋，并给出了几个方向的建议（隐藏一句话/连续点击触发/时间感知/鼠标轨迹/隐藏歌单）。

**我的 Prompt**：

> 认可你说的时间问候+隐藏歌单+隐藏一句话三者组合。三者按发现难度分层：时间问候自动显示（★☆☆）、隐藏歌单需点击音符 ♪（★★☆）、隐藏一句话需悬停 1.5s 才浮现（★★★）。

**AI 回应**：AI 将三个彩蛋整合进页脚设计，并按发现难度形成清晰的层次。后续又加入了贪吃蛇小游戏（连续点击页脚 ~ 五次触发）和彩蛋成就系统（六个成就徽章），让彩蛋从零散的"小惊喜"升级为"系统化"的体验。

**我的判断**：彩蛋的分层设计很好地体现了"温润"——不喧宾夺主，但被有心人发现时会心一笑。

---

### Prompt ⑤：纠正 AI 的理解偏差

**背景**：我要求思考区的卡片"放在屏幕中间"观看更舒适，但 AI 理解成了把"随笔"标题居中固定在屏幕中央，导致标题位置跑偏了。

**我的 Prompt**：

> 不是要随笔两个字放在中间，而是要随笔的卡片放在中间，随笔仍然在顶部。

**AI 回应**：AI 修正了实现——标题恢复自然定位（顶部居中），卡片 track 容器设置 `display:flex + align-items:center + min-height:calc(100vh - 10rem)`，让卡片在标题下方自动垂直居中。

**我的判断**：这是典型的 Vibe Coding 沟通场景——我说"放在中间"，AI 理解错了对象（标题 vs 卡片）。关键在于我看到了效果不对，立即用更精确的语言纠正。这种"发现问题→描述偏差→AI修正"的循环就是 Vibe Coding 的核心能力。

---

### Prompt ⑥：推动视觉升级

**背景**：v2.0 五个模块全部实现后，页面功能已经非常丰富，但整体看起来"太空了"——大量留白，植物太小且缩在角落，装饰语言单调。

**我的 Prompt**：

> 我认为现在网页整体视觉太空了，植物素材在其中太小...需要更加大胆一些，可以大胆的使用线条和几何图形，也可以使用更多的装饰和动态效果。

**AI 回应**：AI 提出了"视觉填充方案"：植物从 60-80px 放大 2~2.5 倍（120-160px），新增边缘竖线、首屏装饰线（菱形）、四角 L 形边框、标题两侧圆点、章节菱形分隔符、卡片折角、联系区双层旋转圆环、背景微图案（网格点阵/斜线纹理/手绘波浪线）等十几项几何装饰。

**我的判断**：这次升级让页面从"朴素精致"变成了"丰富有骨"。几何线条和图形作为视觉骨架，填补了之前的空洞感，但温润调性没有被破坏——所有装饰都是低透明度、淡色系。

---

### Prompt ⑦：手帐背景增强

**背景**：视觉填充之后，整体结构变得更好了，但我觉得背景还可以有更多"手帐/手工"的感觉。

**我的 Prompt**：

> 背景能不能在保留手帐感的同时添加更多元素。

**AI 回应**：AI 提出了六套手帐元素方案（A-F）：
- A. 和纸胶带装饰 —— 4 条半透明胶带斜贴在区域边缘
- B. 印章与邮戳 —— "藏"方框章、星形圆章、"读"方章、年份日期章
- C. 手绘线条 —— 荧光笔下划线、双线边框、圈注椭圆
- D. 碎纸片 —— 5 个淡色长方形飘在背景
- E. 日付与页码 —— 章节分隔符 01-05
- F. 翘角贴纸 —— hover 时翘起的贴纸角

全部用 CSS 实现，无需额外图片。

**我的判断**：这六个方案让页面从"好看的网页"变成了"有手工质感的空间"。胶带和印章尤其加分——它们不是装饰，它们是"有人在这个页面上做手帐"的痕迹，和温润调性完美融合。

---

### Prompt ⑧：细节打磨

**背景**：所有功能就位后，我开始关注实际体验的细节。比如卡片光泽扫过效果，实现后发现实际视觉太亮了。

**我的 Prompt**：

> 卡片光泽扫过效果实际体验比较刺眼，hover 时带上光泽即可。

**AI 回应**：AI 将光照从默认显示改为仅 hover 触发，最高亮度从 `rgba(255,255,255,0.25)` 降至 `0.14`，位移范围缩小，过渡从 0.9s 延长到 1.2s ease-out。同时修复了光照与渐变遮罩共用伪元素的冲突。

**我的判断**：这是 Vibe Coding 中"体验验收"的典型过程——功能正确不一定等于体验好。我需要实际使用页面，发现问题后精确描述，AI 再针对性修复。

---

## 二、踩坑复盘

### 坑 1：思考区在手机上的体验

**发生了什么**：思考区的横向翻阅在桌面端是非常亮眼的招牌交互，但在手机上——屏幕窄、手指天然上下滑——ScrollTrigger 的 pin 机制让用户"卡"在思考区，滚动体验极差。

**怎么发现的**：在设计讨论阶段，AI 主动提到了"ScrollTrigger 在移动端 640px 以下的体验待改善"，但最初版本并没有实际处理。

**怎么解决的**：在 `createThoughtScroll()` 函数开头加入 `window.innerWidth < 640` 的判断，小于 640px 时跳过 pin 和 scrub 创建，卡片降级为正常竖直排列。导航指示器（圆点/箭头/计数器）在手机端也隐藏。

**如果重做会怎么改进**：应该在设计阶段就明确考虑移动端策略，而不是做完桌面版再回头修。一个好的做法是：在 Prompt 中直接要求"请同时给出移动端（<640px）和桌面端的布局方案"。

---

### 坑 2：联系区图标被"盖住"

**发生了什么**：视觉填充阶段，AI 给联系区的社交图标外面加了一个双层旋转虚线圆环（contact-ring），用了 `::before` 和 `::after` 伪元素实现。但因为没有设置 `pointer-events: none`，这两个伪元素像透明玻璃一样盖在图标上方，点击图标毫无反应。

**怎么发现的**：实际点击邮箱/B站/QQ音乐图标时无响应，怀疑是新增的圆环遮挡了点击。

**怎么解决的**：给 `contact-ring::before` 和 `contact-ring::after` 都加上 `pointer-events: none`，让点击事件穿透伪元素到达下方的 `<a>` 链接。

**如果重做会怎么改进**：设计装饰性伪元素时应该默认加上 `pointer-events: none`，养成习惯。另外发现这种问题的方法是"Debug 思维"：新增了什么元素，就往那个方向排查。

---

### 坑 3：回到顶部按钮失效

**发生了什么**：v2.0 优化了回到顶部按钮的体验——要做一个"快速倒带"效果：停顿→加速→减速→名字呼吸动画。AI 用了 GSAP 的 `gsap.to(window, { scrollTo: 0 })` 实现滚动，但这个功能需要 GSAP 的 ScrollToPlugin 插件，项目中没有引入。

**怎么发现的**：点击回到顶部按钮，没有任何反应。控制台没有报错，因为 GSAP 静默忽略了未知的插件调用。

**怎么解决的**：放弃 GSAP ScrollTo 方案，改用浏览器原生的 `window.scrollTo({ top: 0, behavior: 'smooth' })`，滚动到位后通过 `setInterval` 检测位置再触发名字呼吸动画。原生 API 不需要任何插件。

**如果重做会怎么改进**：优先使用浏览器原生 API——`scrollTo`、`IntersectionObserver`、CSS animation 等，避免引入不必要的插件依赖。插件意味着额外的文件大小和潜在的兼容问题。

---

### 坑 4：卡片光泽刺眼 + 与折角效果冲突

**发生了什么**：AI 给所有卡片加了"光照扫过"效果——默认状态下就能看到一条倾斜的光泽。实际效果是卡片表面一直在"闪"，非常刺眼。同时卡片的 `::after` 伪元素被光泽、折角和渐变遮罩三个效果同时使用，互相覆盖。

**怎么发现的**：实际浏览页面时发现卡片视觉过于活跃，光泽一直在闪。

**怎么解决的**：
1. 光泽改为仅 hover 时触发（`opacity: 0 → hover: 1`）
2. 最高白光亮度从 `0.25` 降至 `0.14`
3. 光泽从卡片级的 `::after` 移到内部子元素的伪元素（`thought-card-body::after` / `note-card-img::before`），避免与折角（卡片的 `::before`）和渐变遮罩（`note-card-img::after`）冲突

**如果重做会怎么改进**：做视觉特效时，先问 AI "这个效果的 opacity 和亮度参数有没有更克制的方案"，先做一个低配版，不够再加强。从高配开始调低比反过来更难。

---

### 坑 5：AI 理解错"卡片居中"

**发生了什么**：我说"随笔区卡片放在屏幕中间观看更舒适"，AI 理解为把"随笔"这个标题固定在屏幕中央（`position: fixed; top: 50%; left: 50%`），而不是把卡片纵向居中在标题下方。

**怎么发现的**：AI 实现后标题确实"跳到"了屏幕正中间，遮挡了卡片。"随笔"两个字浮在整个页面上方，明显不对。

**怎么解决的**：用更精确的语言纠正——"不是要随笔两个字放在中间，而是要随笔的卡片放在中间，随笔仍然在顶部"。AI 随即改为在 `thoughts-track-wrapper` 上用 flexbox 的 `align-items: center` 实现卡片垂直居中，标题保持在顶部自然位置。

**关键经验**：Vibe Coding 中，"说错了"不可怕，"说清楚哪里错了"才重要。一句精确的纠错 Prompt 往往比重新提需求更高效。

---

## 三、对 Vibe Coding 的思考

### 我的角色

在整个过程中，我做的事情是：
- **决定方向**：做什么不做什么、什么风格什么感觉
- **判断好坏**：看 AI 的输出，说不满意还是满意、哪里要改
- **描述问题**：看到效果不对时，用具体语言描述偏差

AI 做的事情是：
- **技术实现**：HTML/CSS/JS 代码
- **方案生成**：根据方向提出多个可选方案
- **快速迭代**：根据反馈立即修改

### 有效的 Prompt 策略

| 策略 | 示例 |
|:-----|:-----|
| **比喻法** | "像书页条目一样一页一页略过" → AI 理解了横向翻阅 |
| **否定+精确描述** | "不是标题居中，是卡片居中" → AI 立即修正 |
| **场景化** | "像素游戏机面板：复古卡带插槽风格" → AI 产出具体代码 |
| **感官词** | "刺眼" → AI 降低亮度参数 |

### 无效的 Prompt（我踩过的坑）

- "做得好看一点"——太模糊，AI 不知道往哪个方向改
- "全部重来"——丢失了之前的好的部分
- 不说话，默默让 AI "自查代码"——AI 看不到效果，自查没有意义

### 结语

Vibe Coding 不是"AI 替我做"，而是"我主导方向 + AI 高效执行"。好的 Prompt 像设计 Brief——清晰的方向、具体的约束、可验证的标准。而好的踩坑复盘则是"我在这个过程中学会了什么"。这个主页从最初的空白文件夹到现在的完整作品，每一行代码都经过了我和 AI 的协作——这就是 Vibe Coding 的意义。

---

## 附录：完整对话记录

以上 Prompt 均提取自与 Claude Code (Anthropic Claude 模型) 的完整对话。对话覆盖了：

- 设计定调（色彩/字体/气质）
- 页面结构设计（六大区域 + 横向翻阅）
- 数据可视化（四个兴趣标签主题面板）
- 彩蛋系统（三重彩蛋 + 贪吃蛇 + 成就）
- 视觉填充（几何线条 + 手帐背景）
- 细节打磨（光照/居中/回顶/图标点击）
- 多次迭代修正

完整对话记录可根据需要提供（约 200+ 轮交互）。
